uname -a
lsb_release -a

sudo apt update
sudo apt upgrade -y

sudo apt install -y python3 python3-pip python3-venv python3-dev build-essential git curl

python3 --version
pip3 --version
git --version

pwd
ls -la

python3 -m venv venv
source venv/bin/activate

which python
python -m pip install --upgrade pip
pip --version

---------------------------------------------------------------------

pip install django
django-admin --version
python -m django --version

pip install djangorestframework
pip install django-cors-headers

mkdir backend
cd backend
django-admin startproject config .

python manage.py startapp delivery
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver
CTRL + C

---------------------------------------------------------------------

cd ~/glez-delivery
pwd
npm create vite@latest frontend
npm create vite@latest frontend -- --template react-ts

cd frontend
npm install
npm run dev

npm install axios

cd ~/glez-delivery
git init
touch .gitignore

---------------------------------------------------------------------

pip freeze > requirements.txt
cat requirements.txt
pip install -r requirements.txt

---------------------------------------------------------------------

cd ~/glez-delivery
python --version
pip --version
django-admin --version
node --version
npm --version
git --version
cd backend
python manage.py check


////------------------para cambiar contraseñas de usuarios desde ----------------------------------
python manage.py shell

usuario = User.objects.get(username="Usuario2")
usuario.set_password("123")
usuario.check_password("123")